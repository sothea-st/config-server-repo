server:
  port: 8088  # Or different, if needed

customer:
  service:
    greeting: "Hello from DEV!"

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/customer_db
    username: root
    password: ''
  driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true